/**
 * The category constants for the category module
 */

const STATUS = {
    ACTIVE: "active",
    INACTIVE: "inactive",
    DELETED: "deleted",
    ARCHIVED: "archived",
    CLOSED: "closed"
}

const RESULTS_VISIBILITY = {
    PUBLIC: "public",
    AUTHENTICATED: "authenticated",
    ADMIN_ONLY: "admin_only"
}

export { STATUS, RESULTS_VISIBILITY }
export default STATUS